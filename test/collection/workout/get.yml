info:
  name: get
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/api/workouts/:workoutId
  params:
    - name: workoutId
      value: "{{workout.id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
