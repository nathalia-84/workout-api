info:
  name: delete
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8000/api/workouts/:workoutId
  params:
    - name: workoutId
      value: "{{workouts.0.id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
