info:
  name: update
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8000/api/workouts/:workoutId
  params:
    - name: workoutId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": "{{$randomBs}}",
        "exercises": []
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("workout", res.body)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
