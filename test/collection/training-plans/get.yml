info:
  name: get
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/api/training-plans/:trainingPlanId
  params:
    - name: trainingPlanId
      value: ""
      type: path
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("trainingPlan", res.body)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
