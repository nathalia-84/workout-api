info:
  name: list
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/training-plans
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("trainingPlan", res.body)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
